name: Builds
on:
  push:
    branches:
      - main

    paths-ignore:
      - ".gitignore"
      - "CLAUDE.md"
      - "README.md"

  pull_request:

  release:
    types:
      - published

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

permissions:
  contents: read

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - preset: windows-x86_64-debug
            os: windows-2022
            name: 🏁 Windows Debug
          - preset: windows-x86_64-release
            os: windows-2022
            name: 🏁 Windows Release
          - preset: macos-arm64-debug
            os: macos-latest
            name: 🍎 macOS Debug
          - preset: macos-arm64-release
            os: macos-latest
            name: 🍎 macOS Release
          - preset: linux-x86_64-debug
            os: ubuntu-latest
            name: 🐧 Linux Debug
          - preset: linux-x86_64-release
            os: ubuntu-latest
            name: 🐧 Linux Release

    steps:
      - name: Capture workflow telemetry
        uses: catchpoint/workflow-telemetry-action@v2
        with:
          comment_on_pr: false

      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup CMake
        uses: lukka/get-cmake@4169813123dd72674c1697fc8f2554818db4c92b

      - name: Setup ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: ${{ matrix.preset }}

      - name: Install Godot (Windows)
        if: runner.os == 'Windows'
        run: |
          curl -sL -o godot.zip https://github.com/godotengine/godot/releases/download/4.6.2-stable/Godot_v4.6.2-stable_win64.exe.zip
          unzip -q godot.zip
          mv Godot_v4.6.2-stable_win64.exe "$RUNNER_TEMP/godot.exe"
          echo "GODOT_EXECUTABLE=$RUNNER_TEMP/godot.exe" >> $GITHUB_ENV

      - name: Install Godot (Linux)
        if: runner.os == 'Linux'
        run: |
          curl -sL -o godot.zip https://github.com/godotengine/godot/releases/download/4.6.2-stable/Godot_v4.6.2-stable_linux.x86_64.zip
          unzip -q godot.zip
          mv Godot_v4.6.2-stable_linux.x86_64 "$RUNNER_TEMP/godot"
          echo "GODOT_EXECUTABLE=$RUNNER_TEMP/godot" >> $GITHUB_ENV

      - name: Install Godot (macOS)
        if: runner.os == 'macOS'
        run: |
          curl -sL -o godot.zip https://github.com/godotengine/godot/releases/download/4.6.2-stable/Godot_v4.6.2-stable_macos.universal.zip
          unzip -q godot.zip
          mv Godot.app "$RUNNER_TEMP/Godot.app"
          echo "GODOT_EXECUTABLE=$RUNNER_TEMP/Godot.app/Contents/MacOS/Godot" >> $GITHUB_ENV

      - name: Configure build
        run: cmake --preset ${{ matrix.preset }}

      - name: Build
        run: cmake --build --preset ${{ matrix.preset }} -j 2

      - name: Checking libraries were copied
        run: ls -la */addons/*/bin/

      - name: Run tests
        run: ctest --preset ${{ matrix.preset }}

      - name: Copy documentation
        if: github.event_name != 'pull_request'
        run: |
          cp LICENSE README.md addons/luau_gdextension/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        if: github.event_name != 'pull_request'
        with:
          name: luau-gdextension-${{ matrix.preset }}
          path: |
            addons/luau_gdextension/**

  merge-build-artifacts:
    runs-on: ubuntu-slim
    needs: build
    if: github.event_name == 'push'

    steps:
      - name: Merge artifacts
        uses: actions/upload-artifact/merge@v4
        with:
          name: luau-gdextension
          pattern: luau-gdextension-*
          delete-merged: true

  upload-release-artifacts:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'release'

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v5
        with:
          sparse-checkout: |
            LICENSE
            README.md

      - name: Download merged artifacts
        uses: actions/download-artifact@v5
        with:
          pattern: luau-gdextension-*
          merge-multiple: true
          path: addons/luau_gdextension/

      - name: Zip release
        run: |
          zip -r addons-luau-gdextension.zip addons/

      - name: Upload release asset
        run: |
          gh release upload --repo "${{ github.repository }}" "${{ github.event.release.tag_name }}" addons-luau-gdextension.zip
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
